[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "bash"]

[phases.install]
cmd = "python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt"

[phases.build] 
cmd = "echo 'Python FastAPI backend - no build step required'"

[start]
cmd = "python3 app.py"

[variables]
PORT = "8000"